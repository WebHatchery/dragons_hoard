# Outstanding tasks

- [ ] Move all tests and test-only helpers from `src/` into crate-root `tests/`, including inline reel-chrome tests and the reachability/coverage helpers. Add `src/lib.rs` with deliberate public test seams and make `main.rs` use it. Review the 604 existing test cases by feature: consolidate related inputs toward five cases per feature while preserving regressions and documenting justified exceptions (CODE_STANDARDS §11).
- [ ] Split functions exceeding 100 lines into cohesive helpers, starting with `Game::new`, `GameData::validate`, `actions::apply`, `draw_game_ui`, `draw_spin_button`, and `begin_capture_scene`. Extract responsibilities from the larger modules as needed; keep every Rust file below 800 total physical lines (§2.2, §4.1).
- [ ] Remove the data layer's dependencies on state and engine code: move jackpot/feature-buy semantic validation out of `state` and replace `data/load.rs`'s call to `engine::seam::ladder` with data-owned invariant validation. Preserve rejection of invalid cabinet data (§2.1, §5.3).
- [ ] Move remaining player-facing strings and configurable timing values into typed JSON under `assets/`, loaded through the toolkit. Start with UI labels/shortcut descriptions, rule prose in `state/rules/prose.rs`, celebration text, and `PAYOUT_TIME`/`CASCADE_BEAT` in `state/spin.rs`; validate required text keys and timing ranges (§5.3).
- [ ] Replace direct game-data parsing in hint/achievement tests and `tests/asset_registry.rs::read_json` with toolkit loading APIs. Keep save-serialization tests distinct from game-data loading, and retain malformed-data coverage (§5.3).
- [ ] Make celebration dismissal an explicit visible tap control using the shared pointer/action path, and replace the keyboard-only instruction in `ui/celebration.rs`. Update shortcut/footer copy and `game_page.json` to name visible touch equivalents; correct the advertised Load shortcut from `L` to `K` and describe Enter's focus-dependent behavior accurately (§7.4–7.5).
- [ ] Report discarded persistence errors in `game.rs` and `game/persistence.rs` (wallet, floor, ledger, hints, preferences, achievements, and sessions). Log cabinet-load failures before falling back to the default machine, and surface save failures without falsely reporting success (§6).
- [ ] Align verification scripts and comments with current policy: replace the obsolete “non-test lines” wording in `tests/code_standards.rs` and `verify.ps1`; store verification captures directly in `docs/verification/`, reusing filenames for the same state instead of writing to `%TEMP%`. Correct `scripts/capture_ui.ps1`'s claim that scene names only select filenames (§2.2, §12).
- [ ] Verify the published WASM game in a browser after running parameterless `publish.ps1`: exercise visible controls without a keyboard, tutorials, celebration/feature choices, recovery, save/reload, and desktop/portrait layouts. Check audio startup after a user gesture, music/SFX, and mute/volume behavior. Fix discovered failures and record browser evidence in `docs/verification/`; update the GDD's unverified-browser/audio notes once supported by evidence (§7.5, §8.3, §12).
